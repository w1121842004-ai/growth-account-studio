# ── 本地开发/密钥文件：绝不进镜像 ──
.env
.env.local
.env.production
.env.production.*
*.pem
*.key

# ── 依赖与构建产物：由 Dockerfile 阶段复制，不重复 COPY ──
node_modules
.next
out
dist
build

# ── 测试/文档/CI（design-system 是 globals.css 的构建依赖，必须保留）──
tests
docs
mvp-toutiao-ops
*.log
.DS_Store
.git
.gitignore
